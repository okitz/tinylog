services:
  agent:
    build:
      context: ./agent
      dockerfile: ./.devcontainer/Dockerfile
    volumes:
      - ./agent:/app
    tty: true
  server:
    build:
      context: ./server
      dockerfile: ./.devcontainer/Dockerfile
    volumes:
      - ./server:/app
    tty: true